spring:
  application:
    name: abouhalarodas

  datasource:
    url: jdbc:postgresql://localhost:5432/abouhalarodas
    username: postgres
    password: Churras12.
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080